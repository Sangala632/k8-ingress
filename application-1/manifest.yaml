apiVersion: apps/v1
kind: Deployment
metadata:
  name: app1
  #replica set labels
  labels: 
    purpose: ingress-demo
    project: roboshop
    app: app1
spec:
# These are the labels replica set use to create pod replicas, this should match pod labels
  replicas: 2
  selector:
    matchLabels:
      purpose: ingress-demo
      project: roboshop
      app: app1
  # This is pod definition
  template:
    metadata:
      labels:
        purpose: ingress-demo
        project: roboshop
        app: app1
    spec:
      containers:
      - name: app1  
        image: sangala2025/app-1:v1

---

apiVersion: v1
kind: Service
metadata:
  name: app1-svc
  labels:
    purpose: ingress-demo
    project: roboshop
    app: app1
spec:
  selector:
    purpose: ingress-demo
    project: roboshop
    app: app1
  ports:
    - protocol: TCP
      port: 80 #srevice port
      targetPort: 80 # container port

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app1  
  annotations: 
    alb.ingress.kubernetes.io/listen-ports:  '[{"HTTPS": 443}]' 
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-east-1:838180513114:certificate/0d4ad40d-9703-4ad5-aeee-1ba0b36a379e
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip 

spec:
  ingressClassName: alb
  rules:
  - host: "app1.hellodevsecops.space"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: app1-svc
            port:
              number: 80